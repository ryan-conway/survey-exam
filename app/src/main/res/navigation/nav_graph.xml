<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/loginFragment">

    <fragment
        android:id="@+id/loginFragment"
        android:name="com.example.nimblesurveys.login.LoginFragment"
        android:label="LoginFragment"
        tools:layout="@layout/fragment_login">
        <action
            android:id="@+id/action_login_to_surveyLoading"
            app:destination="@id/surveyLoadingFragment" />
    </fragment>
    <fragment
        android:id="@+id/surveyLoadingFragment"
        android:name="com.example.nimblesurveys.survey.SurveyLoadingFragment"
        android:label="SurveyLoadingFragment"
        tools:layout="@layout/fragment_survey_loading">
        <action
            android:id="@+id/action_surveyLoading_to_surveyList"
            app:destination="@id/surveyListFragment" />
    </fragment>
    <fragment
        android:id="@+id/surveyListFragment"
        android:name="com.example.nimblesurveys.survey.SurveyListFragment"
        android:label="SurveyListFragment"
        tools:layout="@layout/fragment_survey" />
</navigation>